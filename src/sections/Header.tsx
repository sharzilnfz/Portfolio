import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="w-full">
      <nav>
        <div>Header</div>
      </nav>
    </header>
  );
};

export default Header;
